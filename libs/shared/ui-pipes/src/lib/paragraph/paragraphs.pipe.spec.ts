import { ParagraphsPipe } from './paragraphs.pipe';

describe('ParagraphsPipe', () => {
  it('create an instance', () => {
    const pipe = new ParagraphsPipe();
    expect(pipe).toBeTruthy();
  });
});
